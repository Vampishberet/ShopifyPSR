{%- liquid
  assign members = 'PSR Waffle,PSR Vienna,PSR Chimp,PSR Psych,PSR Rev,PSR Dag,PSR Guppy,PSR Blazify,PSR Casper,PSR Cafe,PSR Acsidius,PSR Yum,PSR Nodaz,PSR Light,PSR Zire,PSR Moxi,PSR Ruin,PSR Staryu,PSR Tuhronto,PSR Hapa,PSR Midniii,PSR Channce,PSR Murlo,PSR Rkei,PSR Molly,PSR GazingNicole,PSR Magnus,PSR Ian,PSR Mika,PSR Njuu,PSR Thunder,PSR CeceNuggets,PSR Kaz,PSR Devox,PSR Klamran,PSR Priince,PSR Ryiax,PSR Gards,PSR Sjay,PSR Adrenaleen,PSR Jid,PSR h6rmed,PSR EXR,PSR Korea,PSR Josh,PSR Prioxy,PSR Fancy,PSR Indra,PSR Mei,PSR Volty,PSR Aminz,PSR Rina,PSR Recrim,PSR Teasers,PSR Vampish,PSR Sieon,PSR Mikey,PSR Ache,PSR iGOMA,PSR Gary,PSR Lunar,PSR Twunti,PSR Thibaut,PSR Monster' | split: ','
-%}

<section class="pulsar-ticker-section">
  {%- if section.settings.heading != blank -%}
    <p class="pulsar-ticker-heading">{{ section.settings.heading }}</p>
  {%- endif -%}

  <div class="pulsar-marquee">
    <div class="pulsar-marquee__inner">
      <div class="pulsar-marquee__group">
        {%- for member in members -%}
          <span class="pulsar-marquee__item">{{ member }}</span>
        {%- endfor -%}
      </div>

      <div class="pulsar-marquee__group" aria-hidden="true">
        {%- for member in members -%}
          <span class="pulsar-marquee__item">{{ member }}</span>
        {%- endfor -%}
      </div>
    </div>
  </div>
</section>

<style>
  .pulsar-ticker-section {
    background: #000;
    border-top: 1px solid #222;
    border-bottom: 1px solid #222;
    padding: 16px 0;
    overflow: hidden;
  }

  .pulsar-ticker-heading {
    margin: 0 0 12px;
    text-align: center;
    font-size: 12px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: #999;
  }

  .pulsar-marquee {
    overflow: hidden;
    width: 100%;
  }

  .pulsar-marquee__inner {
    display: flex;
    width: max-content;
    animation: pulsar-scroll 35s linear infinite;
    will-change: transform;
  }

  .pulsar-marquee__group {
    display: flex;
    flex-shrink: 0;
    min-width: max-content;
    align-items: center;
  }

  .pulsar-marquee__item {
    flex: 0 0 auto;
    display: inline-flex;
    align-items: center;
    font-size: 28px;
    line-height: 1;
    text-transform: uppercase;
    color: #d0d0d0;
    padding: 0 24px;
    white-space: nowrap;
  }

  .pulsar-marquee__item::after {
    content: "•";
    margin-left: 24px;
    color: #666;
  }

  .pulsar-marquee:hover .pulsar-marquee__inner {
    animation-play-state: paused;
  }

  @keyframes pulsar-scroll {
    from {
      transform: translateX(0);
    }
    to {
      transform: translateX(-50%);
    }
  }

  @media screen and (max-width: 767px) {
    .pulsar-marquee__item {
      font-size: 20px;
      padding: 0 16px;
    }

    .pulsar-marquee__item::after {
      margin-left: 16px;
    }

    .pulsar-marquee__inner {
      animation-duration: 22s;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .pulsar-marquee__inner {
      animation: none;
    }
  }
</style>

{% schema %}
{
  "name": "Pulsar Member Ribbon",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Section label",
      "default": "Our people"
    }
  ],
  "presets": [
    {
      "name": "Pulsar Member Ribbon"
    }
  ]
}
{% endschema %}