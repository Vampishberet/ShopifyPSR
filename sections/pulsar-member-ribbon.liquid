{%- liquid
  assign members = 'PSR Waffle,PSR Vienna,PSR Chimp,PSR Psych,PSR Rev,PSR Dag,PSR Guppy,PSR Blazify,PSR Casper,PSR Cafe,PSR Acsidius,PSR Yum,PSR Nodaz,PSR Light,PSR Zire,PSR Moxi,PSR Ruin,PSR Staryu,PSR Tuhronto,PSR Hapa,PSR Midniii,PSR Channce,PSR Murlo,PSR Rkei,PSR Molly,PSR GazingNicole,PSR Magnus,PSR Ian,PSR Mika,PSR Njuu,PSR Thunder,PSR CeceNuggets,PSR Kaz,PSR Devox,PSR Klamran,PSR Priince,PSR Ryiax,PSR Gards,PSR Sjay,PSR Adrenaleen,PSR Jid,PSR h6rmed,PSR EXR,PSR Korea,PSR Josh,PSR Prioxy,PSR Fancy,PSR Indra,PSR Mei,PSR Volty,PSR Aminz,PSR Rina,PSR Recrim,PSR Teasers,PSR Vampish,PSR Sieon,PSR Mikey,PSR Ache,PSR iGOMA,PSR Gary,PSR Lunar,PSR Twunti,PSR Thibaut,PSR Monster' | split: ','
-%}

<section class="pulsar-ticker-section">
  {%- if section.settings.heading != blank -%}
    <p class="pulsar-ticker-heading">{{ section.settings.heading }}</p>
  {%- endif -%}

  <div class="pulsar-ticker">
    <div class="pulsar-ticker-track">
      {%- for member in members -%}
        <span class="pulsar-ticker-item">{{ member }}</span>
      {%- endfor -%}

      {%- for member in members -%}
        <span class="pulsar-ticker-item">{{ member }}</span>
      {%- endfor -%}
    </div>
  </div>
</section>

<style>
  .pulsar-ticker-section {
    background: #000;
    border-top: 1px solid #222;
    border-bottom: 1px solid #222;
    padding: 16px 0;
    overflow: hidden;
  }

  .pulsar-ticker-heading {
    margin: 0 0 12px;
    text-align: center;
    font-size: 12px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: #999;
  }

  .pulsar-ticker {
    width: 100%;
    overflow: hidden;
    position: relative;
  }

  .pulsar-ticker-track {
    display: flex;
    width: max-content;
    white-space: nowrap;
    animation: pulsarMarquee 45s linear infinite;
  }

  .pulsar-ticker:hover .pulsar-ticker-track {
    animation-play-state: paused;
  }

  .pulsar-ticker-item {
    flex: 0 0 auto;
    display: inline-flex;
    align-items: center;
    font-size: 28px;
    line-height: 1;
    text-transform: uppercase;
    color: #d0d0d0;
    padding-right: 48px;
  }

  .pulsar-ticker-item::after {
    content: "•";
    margin-left: 48px;
    color: #666;
  }

  @keyframes pulsarMarquee {
    0% {
      transform: translateX(0);
    }
    100% {
      transform: translateX(-50%);
    }
  }

  @media screen and (max-width: 767px) {
    .pulsar-ticker-item {
      font-size: 20px;
      padding-right: 30px;
    }

    .pulsar-ticker-item::after {
      margin-left: 30px;
    }

    .pulsar-ticker-track {
      animation-duration: 30s;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .pulsar-ticker-track {
      animation: none;
      flex-wrap: wrap;
      white-space: normal;
      justify-content: center;
    }
  }
</style>

{% schema %}
{
  "name": "Pulsar Member Ribbon",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Section label",
      "default": "Our people"
    }
  ],
  "presets": [
    {
      "name": "Pulsar Member Ribbon"
    }
  ]
}
{% endschema %}